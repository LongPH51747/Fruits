<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.READ_MEDIA_IMAGES" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Assignment"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">
        <activity
            android:name=".screen.XanNhanDonHangScreen"
            android:exported="false" />
        <!-- <activity -->
        <!-- android:name=".screen.FruitsDetailScreen" -->
        <!-- android:exported="false" /> -->
        <activity
            android:name=".screen.FruitDetailScreen"
            android:exported="false" />
        <activity
            android:name=".screen.AddFruits"
            android:exported="false" />
        <activity
            android:name=".screen.MainActivity"
            android:exported="false" />
        <activity
            android:name=".screen.HomeScreen"
            android:exported="false" />
        <activity
            android:name=".screen.AccountScreen"
            android:exported="false" />
        <activity
            android:name=".screen.RegisterScreen"
            android:exported="false" />
        <activity
            android:name=".screen.LoginScreen"
            android:exported="false" />
        <activity
            android:name=".screen.AdminScreen"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <meta-data
            android:name="preloaded_fonts"
            android:resource="@array/preloaded_fonts" />
    </application>

</manifest>